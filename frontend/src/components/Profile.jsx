export function Profile() {
    return (
        <div style={{ 
            padding: "40px",
            background: "var(--bg-card)",
            borderRadius: "12px"
        }}>
            <h1 style={{ color: "var(--text-primary)" }}>Profile Page</h1>
            <p style={{ color: "var(--text-secondary)" }}>
                This is a placeholder profile page. Add settings later.
            </p>
        </div>
    );
}
